#include <stdlib.h>

#define N 100

int main() {
  int* arr = malloc(sizeof(int) * N);
  for (int i = 0; i < N; i++) arr[i] = i;
  
  int* res =
    map[fuse map-map;]
    (
      \x -> x * x,
      arr,
      N
    );
  
  return 0;
}
